#Decode App - React - RecipeApp

* ```$ git clone https://github.com/clydeinwebdev/decodeapp-react-recipeapp.git```

* ```$ cd decodeapp-react-recipeapp ```

* ```$ npm install -g create-react-app```

